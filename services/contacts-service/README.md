# Contacts Service

Target ownership:

- contacts
- custom fields
- contact tasks
- activity timeline
- contact upsert from events

Current implementation still lives in `services/core-api/app.py` and will be extracted here after scheduling.
